// import request from 'supertest'
// import { app } from './app'

describe('App', () => {
  // beforeAll(async () => {
  //   await app.ready()
  // })

  // afterAll(() => app.close())

  it('should pass', () => {
    expect(1).toBe(1)
  })

  // it('should pass hello world', async () => {
  //   const response = await request(app.server).get('/')
  //   expect(response.status).toBe(200)
  //   expect(response.body).toHaveProperty('hello')
  //   expect(response.body.hello).toEqual('world')
  // })
})
